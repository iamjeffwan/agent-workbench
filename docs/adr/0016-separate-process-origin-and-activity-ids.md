# 区分进程来源编号与当前活动编号

进程来源编号在进程启动时由环境变量带入，进程存活期间保持不变，只回答“谁启动了这个进程”。当前活动编号在进程启动之后，随请求、消息队列任务或其他入口建立，并沿这些传输通道自动传播，回答“当前这次执行由哪次后续触发引起”。

短期命令往往一次执行就结束，进程来源编号即可把程序活动挂到对应的代理工具步骤。长期运行的服务若只用进程来源编号，会把后来的请求或任务错误归到最初的启动命令。因此程序观察同时保存两类编号：能确定当前活动时优先按当前活动编号归属；否则退回进程来源编号，并在界面上保持这一区别可辨。
