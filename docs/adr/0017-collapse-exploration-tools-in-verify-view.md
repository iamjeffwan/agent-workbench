# 验证页默认折叠探查工具

验证页按执行轮次聚合代理工具步骤。`Read`、`Grep`、`Glob` 以及同类文件探查工具默认折叠为「文件探查 × N」，不进入主操作流的逐条展示；`Shell`、`Write`、`Delete`、`Task` 等主操作保持展开，并由 `Shell` 挂载程序边界方法子步骤。

用户可在验证页显式展开探查组查看明细。这样可以保留完整记录，同时避免一次对话中大量低信号工具淹没「工具 → 程序」主叙事。
