# 代理工具调用作为父步骤

Codex 直接发起的工具调用显示为代理工具步骤，并使用 Codex 提供的真实工具名称。由该工具调用启动的进程，以及进程内部产生的边界方法、网络请求和其他程序活动，按照实际触发关系显示为子步骤。这样可以同时区分代理做了什么，以及程序随后做了什么。

当前展示假设是：直接文件工具作为父步骤，一次调用涉及的各个文件变化作为子步骤；选择父步骤查看完整修改，选择文件步骤查看单个文件的差异。该假设等页面设计阶段再确认，不作为固定交互约束。
