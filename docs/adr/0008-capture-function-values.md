# 保存项目函数的参数和结果

边界方法默认保存参数、返回值和错误的值快照，使调用记录不仅说明执行了哪个函数，也能说明处理了什么以及产生了什么。基本类型和普通结构化对象保存实际内容；超大文本、文件、二进制数据、数据流及其他不适合直接保存的对象使用明确标记的摘要或引用。快照设有深度、数量、字节和耗时上限；凭据仍在写入磁盘前隐藏。

记录先进入内存缓冲再批量发送，不在每次调用时同步写盘。缓冲压力过大时，先降低值快照精度；若仍超限，只能阻塞程序以保全记录，或明确标记“记录不完整”，不能静默丢失。
