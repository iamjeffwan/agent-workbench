# Codex 会话文件是当前稳定观察来源

桌面端按会话元数据中的工作目录筛选 Codex 会话文件，并用会话编号和轮次编号组织工具步骤。适配器只读取完整 JSONL 行，保存读取位置，重启后继续同步；Codex 与 Cursor 使用独立来源文件，避免并发覆盖。

当前安装的 Codex 版本没有可依赖的桌面端命令执行前钩子，因此工作台不按时间猜测“工具 → 进程”的因果关系，也不声称已经取得测试前代码状态。桌面状态必须明确显示这项能力不可用。将来只有在执行前钩子能够稳定提供工具编号并允许改写启动参数后，才启用 Codex 的进程来源注入和按轮次代码状态记录。
