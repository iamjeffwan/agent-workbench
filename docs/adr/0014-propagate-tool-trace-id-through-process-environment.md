# 通过进程环境传递进程来源编号

工作台为能够启动程序的 Codex 工具调用分配追踪编号，并在启动进程时将其作为临时环境变量写入，成为该进程的进程来源编号。子进程通过操作系统的环境继承继续携带该编号。进程来源编号只说明“哪个工具启动了这个进程”，用来关联进程级活动，不要求修改项目函数接口。

长期运行的进程在启动后还会处理后续请求或任务；那些活动不能只靠进程来源编号归属，而要使用当前活动编号，见 `0016`。
